// Simple test class for the boilerplate JS class
// remove and add your own!

import adder from '../shared/__boilerplate__'

describe('Adder', () => {
    test('returns the sum of two numbers', () => {
        expect(adder(5, 3)).toEqual(8)
    })
})
